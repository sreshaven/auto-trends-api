---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: autotrends-test-flask-deployment
  labels:
    app: autotrends-test-flask-app
    username: svv346
    env: test
spec:
  replicas: 2
  selector:
    matchLabels:
      app: autotrends-test-flask-app
  template:
    metadata:
      labels:
        app: autotrends-test-flask-app
    spec:
      containers:
        - name: autotrends-test-flask-app
          imagePullPolicy: Always
          image: sreshaven/auto_trends_api:final
          env:
          - name: REDIS_IP
            value: 10.233.15.94
          ports:
          - name: http
            containerPort: 5000
