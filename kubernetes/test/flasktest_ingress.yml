---
kind: Ingress
apiVersion: networking.k8s.io/v1
metadata:
  name: auto-trends-test-ingress
  annotations:
    kubernetes.io/ingress.class: "nginx"
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  rules:
  - host: "otgtest.coe332.tacc.cloud"
    http:
        paths:
        - pathType: Prefix
          path: "/"
          backend:
            service:
              name: flasktest-service-nodeport
              port:
                  number: 31343
